# Create Agent Command Schemas

JSON schemas for structured output from the `/create-agent` command.

## Files

| File | Purpose |
|------|---------|
| (none yet) | Output schemas to be added as needed |

## Schema Categories

Potential schemas for future addition:

- `agent-definition-input.schema.json` - Validation for input definition files
- `quality-report.schema.json` - Structure of Phase 8 quality validation output
- `workflow-state.schema.json` - State between interactive and standard phases

## Related Schemas

These schemas are used by the command but stored in shared locations:

- `.claude/agents/dev-tools/schemas/base-agent.schema.json` - Base schema for all agents
- `.claude/templates/agent-definition-input.template.md` - Input template structure
